# Mejora Del Kit Dev IA

Registrar señales concretas en `inbox.md`. Promover reglas sólo por repetición, retrabajo demostrable o un riesgo alto. Aplicar la skill `refinamiento-kit` y validar con `npm run kit:check` y `npm run kit:test`.
